include "std.goof"

// prints the command line arguments
argv
argc
while dup 0 > do
    over ,64 (ptr) dup strlen
    swap stdout write drop
    "\n" stdout write drop
    1 - swap 8 + swap
end
drop drop
